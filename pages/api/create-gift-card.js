import { createGiftCard } from "../../lib/restAdmin";

export default async function handler(req, res) {
  //   const result = await createGiftCard();
  //   return res.status(200).json({ result });
}
